apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: test-reporter
  annotations:
    config.kubernetes.io/local-config: "true"
    # DO NOT commit real credentials. See /docs/SECRETS.md for production setup.
type: Opaque
stringData:
  username: reporter
  # PLACEHOLDER - Replace with actual password via sealed-secrets or external secrets operator
  password: "CHANGEME"
