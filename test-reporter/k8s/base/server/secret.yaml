apiVersion: v1
kind: Secret
metadata:
  name: server-secret
  namespace: test-reporter
  annotations:
    config.kubernetes.io/local-config: "true"
    # DO NOT commit real credentials. See /docs/SECRETS.md for production setup.
type: Opaque
stringData:
  # PLACEHOLDER - Replace password portion with actual credential
  database_url: "postgres://reporter:CHANGEME@postgres:5432/test_reporter"
