# FastAPI & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Redis
redis>=5.0.1

# Auth — RS256 JWT validation
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0
httpx>=0.26.0

# Legacy — no longer used (auth delegated to auth-service) but kept
# to avoid breaking existing environments / migrations
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0  # Pin to 4.x for passlib compatibility

# Validation & Settings
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Dev & Testing
pytest>=7.4.4
pytest-asyncio>=0.23.3
