apiVersion: v1
kind: Secret
metadata:
  name: postgres-auth-secret
  namespace: sut
  annotations:
    config.kubernetes.io/local-config: "true"
    config.kubernetes.io/needs-update: "password must be replaced with a strong random value"
type: Opaque
stringData:
  # REPLACE THIS VALUE - use sealed-secrets or external secrets operator
  password: "REPLACE-ME-WITH-STRONG-PASSWORD"
