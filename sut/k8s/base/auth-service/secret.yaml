apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: sut
  annotations:
    config.kubernetes.io/local-config: "true"
    config.kubernetes.io/needs-update: "database_url password must be replaced"
type: Opaque
stringData:
  # REPLACE THIS VALUE - password portion must be updated
  database_url: "postgresql://postgres:REPLACE-ME@postgres-auth:5432/auth"
